---
export interface Props {
  title?: string;
  description?: string;
  keywords?: string;
  ogTitle?: string;
  ogDescription?: string;
  ogImage?: string;
  canonicalUrl?: string;
}

const {
  title = "diamo | Dein Körper ist nicht jeden Tag gleich. Dein Training ab jetzt auch nicht mehr.",
  description = "diamo versteht, was dein Körper heute wirklich braucht. Jeden Tag ein Workout, das sich dir anpasst. Hormone, Energie, Schlaf, Stress. Werde jetzt Beta-Testerin.",
  keywords = "personalisiertes Training Frauen, Fitness App Frauen, Workout Energie Schlaf, Frauen Fitness Community, Body Intelligence",
  ogTitle = "diamo | Dein Training passt sich dir an",
  ogDescription = "Jeden Tag ein Workout, das versteht, wie du dich fühlst. Hormone, Energie, Schlaf. Jetzt Early Access sichern.",
  ogImage = "https://diamo.club/diamo-preview.jpg",
  canonicalUrl
} = Astro.props;
---

<title>{title}</title>
<meta name="description" content={description} />
<meta name="keywords" content={keywords} />

<!-- Canonical URL -->
{canonicalUrl && <link rel="canonical" href={canonicalUrl} />}

<!-- Open Graph -->
<meta property="og:title" content={ogTitle || title} />
<meta property="og:description" content={ogDescription || description} />
<meta property="og:type" content="website" />
<meta property="og:image" content={ogImage} />
<meta property="og:locale" content="de_DE" />
<meta property="og:site_name" content="diamo" />
{canonicalUrl && <meta property="og:url" content={canonicalUrl} />}

<!-- Twitter Card -->
<meta name="twitter:card" content="summary_large_image" />
<meta name="twitter:title" content={ogTitle || title} />
<meta name="twitter:description" content={ogDescription || description} />
<meta name="twitter:image" content={ogImage} />

<!-- JSON-LD Structured Data -->
<script type="application/ld+json" set:html={JSON.stringify({
  "@context": "https://schema.org",
  "@type": "Organization",
  "name": "diamo",
  "url": "https://diamo.club",
  "logo": "https://diamo.club/icon.png",
  "description": "Zyklusbasiertes, personalisiertes Fitness-Training für Frauen. Workouts im Einklang mit deiner Biologie und einer echten, lokalen Community.",
  "contactPoint": {
    "@type": "ContactPoint",
    "email": "hello@diamo.club",
    "contactType": "customer service"
  },
  "sameAs": []
})} />

<!-- JSON-LD SoftwareApplication -->
<script type="application/ld+json" set:html={JSON.stringify({
  "@context": "https://schema.org",
  "@type": "SoftwareApplication",
  "name": "diamo",
  "applicationCategory": "HealthApplication",
  "operatingSystem": "iOS, Android",
  "description": "Zyklusbasiertes, personalisiertes Fitness-Training für Frauen. Workouts im Einklang mit deiner Biologie und einer echten, lokalen Community.",
  "url": "https://diamo.club",
  "offers": {
    "@type": "Offer",
    "price": "0",
    "priceCurrency": "EUR"
  }
})} />
